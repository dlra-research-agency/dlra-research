{
  "name": "11ty-starter-template",
  "version": "1.0.0",
  "description": "Minimal 11ty starter for marketing sites, optimized for AWS Amplify",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "keywords": [
    "11ty",
    "eleventy",
    "starter",
    "amplify"
  ],
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "wrangler": "^4.87.0"
  }
}
